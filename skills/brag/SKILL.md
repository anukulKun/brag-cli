---
name: brag
description: Turn the current project website into a short, polished, shareable launch video using Hyperframes. Use when someone says "/brag", "let's brag about this", "make a launch video", "turn this into a video", or wants to share what they built. Reads the project code directly — no live URL or screenshots needed.
---

# /brag

You built it. Now let's brag about it.

**The canonical skill definition lives in [`AGENT.md`](../../AGENT.md) at the repo root.** This file is a thin wrapper for agent-specific skill loaders that require a `SKILL.md` file inside the skill directory.

Follow the instructions in `AGENT.md` for the complete workflow. All references, assets, and scripts are relative to this repo root — `skills/brag/references/`, `skills/brag/assets/`, `skills/brag/scripts/`.
